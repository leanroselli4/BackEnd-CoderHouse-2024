<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1>Carrito de Compras</h1>
        <nav>
            <ul>
                <li><a href="/">Inicio</a></li>
                <li><a href="/users">Gestión de Usuarios</a></li>
                <li><a href="/products">Gestión de Productos</a></li>
                <li><a href="/realtimeproducts">Productos en Tiempo Real</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <h2>Productos en el Carrito</h2>
        <ul>
            {{#each cart.products}}
            <li>{{this.product.name}} - {{this.product.price}} - Cantidad: {{this.quantity}}</li>
            {{/each}}
        </ul>
        <form action="/api/carts/{{cart._id}}/purchase" method="post">
            <button type="submit">Finalizar Compra</button>
        </form>
        {{#if errors}}
        <h3>Errores</h3>
        <ul>
            {{#each errors}}
            <li>Producto {{this}} no pudo ser procesado.</li>
            {{/each}}
        </ul>
        {{/if}}
    </main>
    <footer>
        <p>© 2024 Proyecto de Backend</p>
    </footer>
</body>
</html>


